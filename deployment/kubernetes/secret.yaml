# NEXUS SPECTER PRO — Secrets Template
# ⚠️  Replace base64 values before deploying
# by OPTIMIUM NEXUS LLC
apiVersion: v1
kind: Secret
metadata:
  name: nsp-secrets
  namespace: nexus-specter-pro
type: Opaque
stringData:
  ANTHROPIC_API_KEY:  "REPLACE_ME"
  SHODAN_API_KEY:     "REPLACE_ME"
  CENSYS_API_ID:      "REPLACE_ME"
  CENSYS_API_SECRET:  "REPLACE_ME"
  HUNTER_API_KEY:     "REPLACE_ME"
  GITHUB_TOKEN:       "REPLACE_ME"
  POSTGRES_PASSWORD:  "REPLACE_ME"
  REDIS_PASSWORD:     "REPLACE_ME"
  NSP_SECRET_KEY:     "REPLACE_ME"
  MSF_PASSWORD:       "REPLACE_ME"
